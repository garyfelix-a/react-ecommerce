import React, { createContext } from "react";

const AuthProvider = createContext();

const AuthContext = () => {
    return(
        <div>
            AuthContext
        </div>
    );
}

export default AuthContext;